name: hue_rotation
description: Change the color of any widget that has a color. Like SwiftUI's Hue Rotation.
homepage: https://github.com/nohli/hue_rotation

version: 1.3.0

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=2.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
